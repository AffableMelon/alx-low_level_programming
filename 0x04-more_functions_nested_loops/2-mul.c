#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: var to be multiplied
 * @b: var to be multiplied
 * Return: 0 always
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
